import { createSlice } from "@reduxjs/toolkit";

export const WhoSlice = createSlice({
  name: "who",
  initialState: {
    // ... initial state of this module
  },
  reducers: {
    // ... reducers of this module
  },
});

export default WhoSlice.reducer;
